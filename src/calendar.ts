/** Compatibility façade for the modular calendar implementation. */
export { addMonths, daysInMonth, firstWeekday, fmtRange, fmtTime, longWeekday, monthCells, monthIndex, MONTHS, sameDay, todayYMD, weekdayOf, WEEKDAYS, type YM, type YMD } from './shared/calendar'
export { MonthCalendar } from './molecules/month-calendar'
